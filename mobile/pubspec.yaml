name: guidian_mobile
description: Guidian — AI-Native Adaptive Compliance LMS (mobile client)
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  webview_flutter: ^4.8.0
  just_audio: ^0.9.39
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
